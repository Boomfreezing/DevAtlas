# Atlas Accounts

Small, synthetic account API used only for retrieval evaluation.

## Quick start

```sh
uvicorn src.api:app --host 127.0.0.1 --port 8100
```

The API exposes password login and a health check.
