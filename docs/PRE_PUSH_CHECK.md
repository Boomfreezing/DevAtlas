# 推送前可靠性与整理记录

日期：2026-09-12。本轮基于已有改动继续打磨并准备普通分支推送，不新增菜单、调用生成模型或创建 Release。

## 本轮修正

- 耦合分析区分 TypeScript 类型空间与运行时值空间。`import type`、单项 `type` 导入、类型导出和接口声明不能作为运行时高置信调用证据；混合声明中的正常值绑定继续有效。
- 22 项专项回归覆盖 TS/TSX、默认/命名/命名空间导入、混合导入导出，以及实际名为 `type` 的函数和别名。修复前 14 项失败，修复后全部通过。继承、动态接收者和重导出仍保持保守，不增加未经验证的关系推断。
- 完整验证脚本使用独立数据库、仓库、索引、模型配置、临时目录及 Pytest/Ruff/覆盖率缓存；关闭语义模型联网。成功、跳过 E2E、工具失败或文档校验失败后，均恢复原进程环境，原本不存在的环境变量不会残留为空值。`TEMP`、`TMP`、`TMPDIR` 全部隔离，Python 选择兼容 Windows 和 POSIX 虚拟环境布局；新增 5 项脚本回归，未将命令桩测试冒充为 Linux 真机验收。
- 一键验证纳入离线合成证据回归；前端单测限制为 2 个 worker，未降低断言、覆盖率门槛或放宽超时。CI 在浏览器测试失败时保留追踪文件 7 天。
- 首次完整运行暴露一项文件树测试竞态：文件 DOM 就绪不代表后续窗口化 effect 和观察器已注册。测试现在等待原有 6/7 个观察器条件满足，再执行回收与恢复断言；保留所有数量、断连和请求数检查，未修改产品实现。

## 文件整理与公开边界

- [文档导航](./README.md) 按使用维护、正确性证据、性能记录和交付资料分组；[交付清单](./DELIVERY.md) 区分常规回归、真实语料评测与 Release。
- README 明确：规则报告和结构分析无需模型，智能问答必须先配置生成模型；嵌入模型不能替代生成模型。
- 6 份文档移除个人绝对临时路径，1 份性能摘要只改运行目录元数据为项目相对路径。历史指标、日期与实现指纹未改写，也未重新标注为本轮测量。
- 暂存复核时删除一份历史检索评测 JSON 的空白尾行；修正前后数据内容完全相同。
- 扩展忽略规则覆盖本地环境变量文件、数据库旁路文件、覆盖率分片与 Ruff 缓存，保留 `.env.example`。
- 仅提交代码、测试、文档、固定标注和经过审查的评测摘要；日常数据库、真实语料副本、模型、API 配置和运行目录继续忽略。保留本地排障记录，不进行批量删除。

## 本轮验证

最终测试数量见 [README](../README.md#可量化结果)。完整命令为项目根目录的 `scripts/verify_project.ps1`，需预先安装依赖；真实耦合抽查另外从 `backend/` 执行 `python -m evaluations.coupling_real`，只校验既有本地语料。

| 检查 | 本轮最终结果 |
|---|---|
| 文档链接、Ruff | 通过 |
| 后端测试 | 460 项通过；覆盖率 86.92%，严格资源警告检查通过 |
| 合成检索与引用 | 36/36；124 条引用均通过合法性校验，不代表生成回答质量 |
| 前端单测 | 25 个文件、340 项通过，2 个 worker |
| 生产构建 | TypeScript 与 Vite 构建通过 |
| 浏览器回归 | 42 项通过（1 项隔离真实主流程、41 项模拟 API 回归） |
| 真实耦合定向探针 | 24/24，详情见下文摘要 |

最终完整脚本成功退出。验证数据位于项目内 `data/tmp/verification/run-6daafb8773d74ce59bec9392746493c8`，不加入 Git；本轮未访问真实生成服务。

固定 Flask、Requests、Express 共 24 条探针复测全部通过：15 条应确认关系被确认、9 条不应确认关系被排除，没有失效行号或运行期源码/实现指纹漂移。[本轮原始摘要](../benchmarks/coupling_real/prepush-2026-09-12.json) 与[上一轮摘要](../benchmarks/coupling_real/recheck-2026-09-12.json) 分开保留，不覆盖历史结果。

这些是维护者编写的定向探针，不是独立留出集，也不能证明整个调用图的精确率、召回率或风险分数与真实缺陷概率的关系。真实模型回答和新机器冷启动未在本轮验证；性能基准没有在本轮重新测量。

提交 `41113a0` 验收时仍有一条来自 Starlette 测试客户端/httpx 的依赖弃用提示，不是当时的应用测试失败；该历史事实保留，不改写为当时“零警告”。后续工程欠账处理中已将 Starlette 1.6 优先使用的 `httpx2>=2.0,<3.0` 加入开发依赖，生产网络请求仍使用 `httpx>=0.28,<1.0`。Pytest 现在把 `StarletteDeprecationWarning` 视为错误，依赖缺失或后续再次使用弃用接口会直接使测试失败，而不是被静默屏蔽。

该工程欠账修复先在缺少 `httpx2` 的环境中确认警告门禁会失败；安装声明的新开发依赖后，API 专项 37 项及后端全量 460 项通过，全量覆盖率 87.05%，输出不再包含该弃用警告。`pip check` 与 Ruff 同时通过。没有迁移生产 HTTP 客户端、修改业务代码或调用外部模型。

## 后续建议

优先在现有功能内补充用户实际遇到的错例与跨平台回归，再扩大耦合证据边界。模型回答验收、增加模块/功能、选择项目许可证或发布版本标签，都应单独确认范围。本记录描述本地检查；远端 CI 是否成功须以具体提交的 GitHub Actions 结果为准。
