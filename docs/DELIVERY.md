# DevAtlas 发布与简历交付清单

## 仓库内容

- [x] 根目录 README 包含定位、技术栈、启动、测试和API入口。
- [x] `docs/` 集中保存架构、计划、性能、演示和截图。
- [x] 本地开发与 Docker 统一使用根目录 `data/`。
- [x] `.env.example` 提供无敏感信息的配置模板。
- [x] 数据库、导入仓库、API Key、依赖和构建产物已加入 `.gitignore`。
- [ ] 添加开源许可证，并确认第三方依赖许可证兼容性。

## 自动化验证

- [x] 后端：29项 Pytest 测试通过，当前总覆盖率85%。
- [x] 前端：7项 Vitest 测试通过。
- [x] 端到端：Playwright 覆盖导入、报告、搜索和增量分析主流程。
- [x] 前端生产构建通过。
- [x] GitHub Actions 包含后端、前端和端到端任务。
- [x] `scripts/verify_project.ps1` 可从根目录执行文档链接和完整测试验证。

## 发布前隐私检查

- [ ] 检查 `git status`，确认没有 `data/*.db`、`data/repositories/` 或 `.env`。
- [ ] 检查截图中没有本地绝对路径、个人仓库名称、API Key或通知信息。
- [ ] 使用全新目录克隆并按 README 完成一次冷启动。
- [ ] 验证 Ollama/OpenAI 配置失败时不会向前端回传密钥。

## 简历材料

- [x] 系统架构说明。
- [x] 可复现性能评测。
- [x] 2～3分钟演示脚本。
- [x] 更新最新UI截图。
- [ ] 录制2～3分钟演示视频。
- [x] 创建首个正式 Git 提交和 `v0.8.0` 版本标签。

## 当前可使用的实测数据

- 中型项目：26个文件、9,553行代码。
- 全量重新分析中位数：167.97ms。
- 无变化增量分析中位数：10.90ms。
- 同口径最高加速比：15.41×。
- 大型项目：929个文件、40,515行代码；无变化增量分析中位数287.54ms。

性能数字的适用边界和原始样本参见 [PERFORMANCE.md](./PERFORMANCE.md)。
